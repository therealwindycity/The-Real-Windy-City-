"""Windy City Reaper — Civic Accountability Edition.

Tiered, robots-aware civic-records engine: official APIs > RSS > HTML >
rendered HTML, with every blocked source routed to a statutory records request
instead of a dead end. Every emitted event carries a verbatim quote, a source
URL and a fetch timestamp.
"""

__version__ = "1.0.0"
